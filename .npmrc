# pnpm settings
# Hoist nothing by default — keeps the dependency tree strict so code can only
# import packages it actually declares. This catches phantom dependencies early.
auto-install-peers=true
